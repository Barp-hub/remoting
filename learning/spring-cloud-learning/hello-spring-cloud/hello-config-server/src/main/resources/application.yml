server:
  port: 10000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spring-cloud-samples/config-repo

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/